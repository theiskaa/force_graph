name: force_graph
description: "A force-directed graph widget for Flutter — a faithful d3-force port with charge, link, and collide physics."
version: 0.1.0
homepage: https://github.com/theiskaa/force_graph
repository: https://github.com/theiskaa/force_graph

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
